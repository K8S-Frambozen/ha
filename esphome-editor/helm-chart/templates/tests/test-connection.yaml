apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esphome-editor.fullname" . }}-test-connection"
  labels:
    {{- include "esphome-editor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esphome-editor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
