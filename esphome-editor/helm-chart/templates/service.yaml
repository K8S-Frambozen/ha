apiVersion: v1
kind: Service
metadata:
  name: {{ include "esphome-editor.fullname" . }}
  labels:
    {{- include "esphome-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esphome-editor.selectorLabels" . | nindent 4 }}
