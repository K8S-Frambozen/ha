apiVersion: v1
kind: Service
metadata:
  name: {{ include "matter-hub.fullname" . }}
  labels:
    {{- include "matter-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "matter-hub.selectorLabels" . | nindent 4 }}
    type: matter
  ports:
    - name: matter
      port: {{ .Values.service.port.matter }}
      protocol: TCP
      targetPort: matter